# Description:
#   Kingdon's callbacks
#
# Dependencies:
#   None
#
# Configuration:
#   None
#
# Commands:
#   tebot sup dawg - Receive a pug
#   hubot pug bomb N - get N pugs

dumpObj = (obj) ->
  o = "{"
  for own key, value of obj
    v = value
    if key == "robot" || key == "envelope"
      v = dumpObj value
    o = o + "#{key}: #{v},"
  o = o + "}"

module.exports = (robot) ->

  robot.respond /sup dawg/i, (msg) ->
    msg.http("http://pugme.herokuapp.com/random")
      .get() (err, res, body) ->
        msg.send JSON.parse(body).pug

  robot.respond /save\s+(.*)/i, (res) ->
    robot.brain.set 'memory', res.match[1]
    res.send "OK, got it."
    
  robot.respond /recall/i, (msg) ->
    memory = robot.brain.get 'memory'
    msg.send memory

  robot.respond /dump\s*(.*)/i, (res) ->
    o = dumpObj res
    res.send o
